<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:id="@+id/health_info_item_layout"
    android:layout_margin="2dp"
    android:background="#ccc"
    >

    <ImageView
        android:id="@+id/health_info_item_img"
        android:layout_width="0dp"
        android:layout_height="100dp"
        android:layout_weight="1"
        tools:src="@mipmap/ic_launcher_round"
        />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="100dp"
        android:layout_weight="2.5"
        android:orientation="vertical"
        android:padding="10dp"
        >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="3"
            android:orientation="vertical">

            <TextView
                android:id="@+id/health_info_item_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="10dp"
                tools:text="Title"
                />

            <TextView
                android:id="@+id/health_info_item_summary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="This is summary of health info article"
                />
        </LinearLayout>

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_gravity="bottom"
            android:layout_weight="1.1"
            >


            <TextView
                android:id="@+id/health_info_item_comment"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_alignParentRight="true"
                android:drawableLeft="@drawable/ic_comment"
                android:drawablePadding="5dp"
                android:drawableStart="@drawable/ic_comment"
                android:gravity="center"
                tools:text="8"/>

            <TextView
                android:id="@+id/health_info_item_like_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginRight="5dp"
                android:layout_toLeftOf="@id/health_info_item_comment"
                android:drawableLeft="@drawable/bg"
                android:drawablePadding="5dp"
                android:drawableStart="@drawable/bg"
                android:gravity="center"
                tools:text="10"
                />
        </RelativeLayout>
    </LinearLayout>
</LinearLayout>